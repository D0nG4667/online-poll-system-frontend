# The real Django backend URL (used by the Next.js proxy server-side)
# DO NOT prefix with NEXT_PUBLIC_ to keep it secure.
BACKEND_URL=http://localhost:8000

# The frontend uses relative paths like '/allauth' or '/api/v1', 
# which are handled by the Next.js server.
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3001
